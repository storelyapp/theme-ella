{%- layout none -%}
                    {%- capture results -%}
                    {
                        {%- for collection in collections -%}
                            {{-collection.handle | json-}}:{{- collection.all_vendors|json -}}
                            {%- unless forloop.last -%},{%- endunless -%}
                        {%- endfor -%}
                    }
                    {%- endcapture -%}
                    {
                       "results": {{- results -}}
                    }