{%comment%}
                    {%- layout none -%}
                    {
                        "results": {{-collections["size-black"].tags | json-}}
                    }
                    {%endcomment%}
                    {%- layout none -%}
                    {%- capture results -%}
                    {
                        {%- for collection in collections -%}
                            {{-collection.handle | json-}}:{{- collection.tags|json -}}
                            {%- unless forloop.last -%},{%- endunless -%}
                        {%- endfor -%}
                    }
                    {%- endcapture -%}
                    {
                       "results": {{- results -}}
                    }